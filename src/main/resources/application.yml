spring:
  application:
    name: capstone-crawler

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3307}/${MYSQL_DATABASE:capstone_db}
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:rkwhr123}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 6000ms
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

server:
  port: ${SERVER_PORT:8080}

# AI 서버 설정
ai:
  server:
    url: ${AI_SERVER_URL:http://15.164.30.147:8000}

# 애플리케이션 콜백 URL 설정
app:
  callback:
    base-url: ${APP_CALLBACK_BASE_URL:https://cb4ff1b1f029be.lhr.life}

# Playwright 브라우저 풀 설정
playwright:
  pool:
    size: 3  # 로컬 개발 환경

logging:
  level:
    com.example.capstone_java: ${LOG_LEVEL:DEBUG}
    org.springframework.kafka: INFO
    org.apache.kafka: WARN  # Kafka Client 로그 줄이기